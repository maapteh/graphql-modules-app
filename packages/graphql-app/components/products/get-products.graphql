query getProducts($id: String!) {
    getProducts(id: $id) {
        products {
            id
            title
            shortDescription
            images {
                key
                url
            }
            urls {
                key
                value
            }
        }
    }
}
